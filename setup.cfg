[aliases]
test = pytest

[metadata]
description-file = README.rst

[tool:pytest]
addopts =
    --verbose
    --cov=mri
    --cov-config=.coveragerc
    --ignore-glob='*test_local*.py'
    --cov-report=term
    --cov-report=xml
    --junitxml=pytest.xml
testpaths = mri/tests/
